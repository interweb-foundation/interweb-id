import { SimpleLayoutType } from '../types';
export declare const SimpleLayout: ({ size, logo, connectButton, links, customLink, chainDropdown, copyAddressButton, isFullWidth, children }: SimpleLayoutType) => JSX.Element;
