import { MainLayoutPropsType } from '../types';
export declare const MainLayout: ({ children, logo, linkList, connectButton, selectChainDropdown, copyAddressButton, connectedUserCard }: MainLayoutPropsType) => JSX.Element;
